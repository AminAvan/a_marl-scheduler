## The config of the jobshop-test-scenario environment
#name: JobShop-v0
#task_name: jobshop-test-scenario
#
#task_config:
#  num_jobs: 5 # number of jobs that need to be scheduled.
#  num_machines: 4 # number of machines that the jobs can be scheduled on.
#  max_num_ops: 4 # maximum number of operations for any given job.
#  max_op_duration: 4 # maximum processing time of any given operation.
#  # the above values is derived from 'jumanji/environments/packing/job_shop/generator.py'
## Remove env_kwargs/time_limit from you config file, jobshop doesn't take that argument
#env_kwargs: {}
##   time_limit: 80  # time_limit ≥ num_jobs × max_num_ops × max_op_duration
##                    # time_limit ≥ 5 * 4 * 4
#######################################################
########### above code working fine ###################
#######################################################
# Configuration for the jobshop-test-scenario environment in Mava
# Configuration for the jobshop-test-scenario environment in Mava
name: JobShop-v0
task_name: jobshop-test-scenario

task_config:
  num_jobs: 5
  num_machines: 4
  max_num_ops: 4
  max_op_duration: 4

env_kwargs: {}

network:
  num_layers_machines: 1
  num_layers_operations: 1
  num_layers_joint_machines_jobs: 2
  transformer_num_heads: 8
  transformer_key_size: 16
  transformer_mlp_units: [512]

training:
  num_epochs: 65
  num_learner_steps_per_epoch: 200
  n_steps: 10
  total_batch_size: 128

evaluation:
  eval_total_batch_size: 512
  greedy_eval_total_batch_size: 512

a2c:
  normalize_advantage: False
  discount_factor: 0.99
  bootstrapping_factor: 0.95
  l_pg: 1.0
  l_td: 1.0
  l_en: 0.5
  learning_rate: 1e-4