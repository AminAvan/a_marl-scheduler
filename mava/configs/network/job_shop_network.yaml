defaults:
  - _self_

networks:
  actor:
    class: mava.networks.job_shop_network.JobShopActor
    torso: mava.networks.job_shop_network.JobShopEncoder
    action_head: mava.networks.heads.CategoricalHead
  critic:
    class: mava.networks.job_shop_network.JobShopCritic
    torso: mava.networks.job_shop_network.JobShopEncoder
    centralised_critic: False